// import { LOADED, LOADING, ERROR } from "../../../middleware/actions"

// import { LOGOUT } from "./actionNames"
// import { selectData } from "./selectors"

//  Required variables
export const initialState = {
    /* Holds the user redux object */
    data: null,
    /* Are we loading new checklist items */
    isLoading: false,
    /* Loading checklist items resulted in an error */
    loadingError: null,
}


// Reducer
export const reducers = {
    // Reset reducers
    // [LOGOUT + LOADED]() {
    //     return initialState
    // },
}
