import React from "react"
import Loading from "./Loading"

export default {
    storyCategory: "Base Components",
    storyName: "Loading",
    storyFn() {
        return (
            <div>
                <Loading />
            </div>
        )
    },
}
